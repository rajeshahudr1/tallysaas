{
  "name": "teloora-web",
  "version": "1.0.0",
  "private": true,
  "description": "Teloora (Connected Accounting) SaaS — Phase 1 UI-only web tier (Express + EJS + Bootstrap 5). Renders screens from mock data; the mock layer will later be swapped for an apiClient that calls the REST API.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "tally",
    "cloud-sync",
    "saas",
    "express",
    "ejs",
    "bootstrap",
    "ui"
  ],
  "author": "Teloora",
  "license": "UNLICENSED",
  "dependencies": {
    "compression": "^1.7.4",
    "connect-pg-simple": "^10.0.0",
    "dotenv": "^17.4.2",
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "express-ejs-layouts": "^2.5.1",
    "express-session": "^1.19.0",
    "form-data": "^4.0.6",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "multer": "^2.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
